{
  "name": "twitch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "wrangler:dev": "wrangler dev",
    "deploy": "bash scripts/deploy.sh",
    "deploy:check": "node scripts/check-deploy-token.mjs",
    "upload-images": "bash scripts/upload-images.sh",
    "migrate": "wrangler d1 migrations apply fungaming-rentals --remote",
    "migrate:local": "wrangler d1 migrations apply fungaming-rentals --local",
    "migrate:list": "wrangler d1 migrations list fungaming-rentals --remote"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "steam-user": "^5.3.0"
  }
}
